CC=mpicc
FC=mpif90

BASE_CFLAGS = -g -O3 -mmic -Ofast -fno-alias -openmp #-mcmodel=large

CFLAGS=$(BASE_CFLAGS) ${LIKWID_INC} -DLIKWID -DLIKWID_PERFMON
VCFLAGS += -mmic ${LIKWID_INC} -DLIKWID -DLIKWID_PERFMON
LDFLAGS = ${LIKWID_LIB} -llikwid

